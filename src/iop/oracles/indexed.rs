//! Re-exports of the canonical IndexedOracle types from `ark_iop`.
//! Existed locally during the extraction; canonical definitions now
//! live in the ark-iop crate.

pub use ark_iop::oracle::indexed::{IndexedOracle, ValidatedOracle};
